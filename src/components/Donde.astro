<style>
    p.donde-info {
      margin-bottom: 1rem !important;
    }
  .donde-section {
    max-width: 1100px;
    margin: 3rem auto;
    padding: 2rem 0;
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 1.2s, transform 1.2s;
  }
  .donde-section.visible {
    opacity: 1;
    transform: none;
  }
  .donde-title {
    text-align: center;
    font-family: 'Abril Fatface', serif;
    font-size: 2.8rem;
    margin-bottom: 2.2rem;
    color: #000;
    letter-spacing: -1px;
    transition: color 0.3s;
    cursor: pointer;
  }
  .donde-title:hover {
    color: #ffb006;
  }
  .donde-info {
    font-size: 1.45rem;
    color: #444;
    margin-bottom: 1rem;
    transition: color 0.3s;
    cursor: pointer;
  }
  .donde-info:hover {
    color: #ffb006;
  }
  .donde-info strong {
    color: #8d5524;
    font-size: 1.18rem;
    transition: color 0.3s;
    cursor: pointer;
  }
  .donde-info strong:hover {
    color: #ffb006;
  }
  .donde-mapa {
    border-radius: 16px;
    box-shadow: none;
    transition: box-shadow 0.5s, transform 0.5s;
    overflow: hidden;
  }
  .donde-mapa:hover {
    box-shadow: 0 8px 32px #ffb00622;
    transform: scale(1.03);
  }
  @media (max-width: 900px) {
    .donde-section {
      padding: 0.5rem 0.1rem !important;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .donde-title {
      font-size: 1.5rem !important;
      margin-bottom: 0.5rem !important;
      text-align: center !important;
    }
    .donde-flex {
      flex-direction: column !important;
      gap: 0 !important;
      padding: 0 !important;
      align-items: center !important;
      display: flex !important;
      width: 100%;
      max-width: 420px;
      margin: 0 auto;
      box-shadow: 0 2px 16px #ffb00622;
      border-radius: 16px;
      background: #fff8ea;
      overflow: hidden;
      /* Elimina espacio entre hijos */
    }
    .donde-flex > div {
      padding: 0 !important;
      margin: 0 !important;
      height: auto !important;
      min-height: unset !important;
      box-sizing: border-box;
      flex: unset !important;
      min-width: unset !important;
      max-width: 100% !important;
      width: 100% !important;
    }
    .donde-info {
      font-size: 1rem !important;
      background: transparent;
      border-radius: 0;
      padding: 0.7rem 0.7rem 0 0.7rem;
      margin-bottom: 0 !important;
      box-shadow: none;
      text-align: center !important;
      border-bottom-left-radius: 0 !important;
      border-bottom-right-radius: 0 !important;
    }
    .donde-mapa {
      max-width: 100% !important;
      min-width: 0 !important;
      border-radius: 0 0 16px 16px !important;
      box-shadow: none !important;
      overflow: hidden !important;
      margin-bottom: 0 !important;
      margin-top: 0 !important;
      border-top-left-radius: 0 !important;
      border-top-right-radius: 0 !important;
      /* Elimina espacio superior */
    }
    .donde-mapa iframe {
      height: 240px !important;
      border-radius: 0 0 16px 16px !important;
      width: 100% !important;
      display: block;
    }
  }
</style>
<section class="donde-section">
  <h2 class="donde-title">¿Dónde nos ubicamos?</h2>
  <div class="donde-flex" style="display: flex; flex-wrap: wrap; gap: 2rem; align-items: center; justify-content: center;">
    <div style="flex: 1 1 300px; min-width: 280px;">
      <p class="donde-info">
        Nos encontramos en el corazón de Santa Anita.<br>
        <strong>Dirección:</strong> Av.Francisco Bolognesi 784, Santa Anita, Lima<br>
        <strong>Horario:</strong> Miercoles a Viernes, 5:30 pm - 11:00 pm<br>
      </p>
    </div>
    <div style="flex: 1 1 350px; min-width: 280px;">
      <div class="donde-mapa">
        <iframe
          src="https://www.google.com/maps/embed?pb=!4v1756716671263!6m8!1m7!1sr9GbSKSUXiiPf6W5m-tDag!2m2!1d-12.04892557597536!2d-76.97235696625327!3f87.25208716538704!4f-14.812003549662208!5f1.2438506758589454"
          width="100%"
          height="300"
          style="border:0; border-radius: 0; box-shadow: none;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Street View Anticuchería Tío Memo"
        ></iframe>
      </div>
</section>
<script>
  window.addEventListener('DOMContentLoaded', () => {
    const section = document.querySelector('.donde-section');
    if (!section) return;
    const observer = new window.IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        } else {
          entry.target.classList.remove('visible');
        }
      });
    }, { threshold: 0.15 });
    observer.observe(section);
  });
</script>
    </div>
  </div>
</section>